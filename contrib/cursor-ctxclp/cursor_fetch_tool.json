{
  "name": "ctxclp_fetch",
  "description": "Retrieve the full uncompressed output of a previous ctxclp_run call. Use this when you need more detail than the compressed output provides. The uuid comes from the [CTXCLP:raw=<uuid>] footer in ctxclp_run output.",
  "parameters": {
    "type": "object",
    "properties": {
      "uuid": {
        "type": "string",
        "description": "The output ID from a [CTXCLP:raw=<uuid>] footer"
      }
    },
    "required": ["uuid"]
  },
  "implementation": {
    "type": "shell",
    "command": "ctxclp fetch {{{uuid}}}"
  }
}
